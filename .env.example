# Optional: enables model-generated analyst narratives in the Copilot panel.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Optional: enriches public IPs, domains, and hashes with VirusTotal data.
VIRUSTOTAL_API_KEY=

# Optional: keep the SQLite demo data outside the repository.
# DATA_DIR=./app/data

